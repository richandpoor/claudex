name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  bats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash syntax check
        run: bash -n bin/claude-acct && bash -n bin/claudex
      - name: bats
        run: test/run.sh
